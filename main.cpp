#include <iostream>
#include "Player.hpp"

int main()
{
    // creer une classe Game ou je ne sais quel nom qui contient un la carte et un tableau de player

    // Recupérer le nombre jouers
    // Boucle le nombre de fois necesssaire afin d'ajouter chaque nouveau player
    // Créer la carte en focntion du nombre de player
    // faire une boucle while pour le nombre de tour avec boucle for pour chauqe jouer pour chaque tour
    Player player;
    std::string text = "bhevbehbvhbvehbvre";
    player.show_text(text);
    player.displayPlayerInfo();
    return 0;
}
